import { json } from "@remix-run/node";
import { Link, useLoaderData } from "@remix-run/react";


export default function Get() {
    /* 
    https://api.publicapis.org/
    There is /entries, /random, /categories & /health
    */
    return(
        <main>
        </main>
    )
}